# 更新日志

## [1.2.0] - 2026/09/10

### 新增

- 新增 FTB Quests 任务书汉化支持，自动识别内联式 SNBT、本地化键、外部语言文件和混合模式。
- 本地化键模式会生成 `assets/ftbquests/lang/zh_cn.json`，与模组汉化合并为同一个资源包。
- 内联式或外部语言文件模式会生成完整 `quests` 目录，并可直接替换整合包中的任务书数据。
- 新增一键导入功能：自动将原 `quests` 备份为 `quests_backup`，复制新任务书，并在失败时回滚。
- GUI 和 CLI 新增 FTB Quests 目录、输出目录、自动检测和 `--ftb-auto-import` 等配置。
- 支持从 `config/ftbquests/quests/lang` 和整合包 `resourcepacks` 读取本地化键的英文原文。
- 已有中文任务书内容会被保留，再次运行时可继续补齐新增文本。

### 改进

- FTB Quests 产物优先使用目录而不是 ZIP，避免压缩与解压，提高生成和一键替换速度。
- 优化内联文本替换流程，保持原 SNBT 文件结构和非目标内容不变。
- 更新配置模板、构建配置、发布脚本、README 和冒烟测试。
- 完善资源包安全写入、任务书目录覆盖保护、备份冲突处理和错误回滚。

## [1.1.0] - 2026/09/10

### 初始版本

- 支持 GUI 和 CLI 两种运行方式。
- 支持扫描 Forge、Fabric 等模组的 JAR 语言文件。
- 支持 AI、百度翻译和 DeepL 翻译接口。
- 支持翻译缓存、原文去重、批量请求和并发控制。
- 支持自动跳过已有中文模组，并可强制重新翻译。
- 支持根据 Minecraft 版本选择资源包 `pack_format`。
- 支持安全取消、原子写入和资源包验证。
- 支持打包为单文件 EXE 并生成发布 ZIP。
